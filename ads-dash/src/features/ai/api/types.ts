// Tipos públicos da feature `ai` — diretriz/cérebro da marca.

export interface AiBrain {
  norte: string;
  publico_alvo: string;
  tom_de_voz: string;
  ofertas_atuais: string;
  diferenciais: string;
  evitar: string;
  palavras_chave: string;
}
